diff --git a/clients/meson.build b/clients/meson.build
index 2b96aa5..850b6aa 100644
--- a/clients/meson.build
+++ b/clients/meson.build
@@ -23,25 +23,29 @@ srcs_toytoolkit = [
 	viewporter_client_protocol_h,
 	viewporter_protocol_c,
 ]
-deps_toytoolkit = [
-	dep_wayland_client,
-	dep_lib_cairo_shared,
-	dep_matrix_c,
-	dep_xkbcommon,
-	dependency('wayland-cursor'),
-	cc.find_library('util'),
-]
-lib_toytoolkit = static_library(
-	'toytoolkit',
-	srcs_toytoolkit,
-	include_directories: common_inc,
-	dependencies: deps_toytoolkit,
-	install: false,
-)
-dep_toytoolkit = declare_dependency(
-	link_with: lib_toytoolkit,
-	dependencies: deps_toytoolkit,
-)
+if dep_lib_cairo_shared.found()
+	deps_toytoolkit = [
+		dep_wayland_client,
+		dep_lib_cairo_shared,
+		dep_matrix_c,
+		dep_xkbcommon,
+		dependency('wayland-cursor'),
+		cc.find_library('util'),
+	]
+	lib_toytoolkit = static_library(
+		'toytoolkit',
+		srcs_toytoolkit,
+		include_directories: common_inc,
+		dependencies: deps_toytoolkit,
+		install: false,
+	)
+	dep_toytoolkit = declare_dependency(
+		link_with: lib_toytoolkit,
+		dependencies: deps_toytoolkit,
+	)
+else
+	dep_toytoolkit = dependency('', required: false)
+endif
 dep_gbm = dependency('gbm', required: false, version: '>= 21.3')
 
 simple_clients_enabled = get_option('simple-clients')
@@ -497,7 +501,7 @@ if get_option('shell-desktop')
 	env_modmap += 'weston-desktop-shell=@0@;'.format(exe_shell_desktop.full_path())
 endif
 
-if get_option('shell-desktop') or get_option('shell-kiosk') or get_option('shell-ivi')
+if dep_toytoolkit.found() and (get_option('shell-desktop') or get_option('shell-kiosk') or get_option('shell-ivi'))
 	exe_shooter = executable(
 		'weston-screenshooter',
 		'screenshot.c',
diff --git a/frontend/meson.build b/frontend/meson.build
index 908e36e..4273927 100644
--- a/frontend/meson.build
+++ b/frontend/meson.build
@@ -41,11 +41,16 @@ dep_libexec_weston = declare_dependency(
 	include_directories: [ include_directories('.'), public_inc ],
 	dependencies: dep_libweston_public
 )
+exe_weston_link_args = []
+if cc.get_define('__cervus__') != ''
+	exe_weston_link_args += [ '-Wl,--whole-archive', '-lcervus_pic', '-Wl,--no-whole-archive' ]
+endif
 exe_weston = executable(
 	'weston',
 	'executable.c',
 	include_directories: common_inc,
 	dependencies: dep_libexec_weston,
+	link_args: exe_weston_link_args,
 	install_rpath: dir_module_weston,
 	install: true
 )
diff --git a/shared/meson.build b/shared/meson.build
index eb2f30d..56f27b2 100644
--- a/shared/meson.build
+++ b/shared/meson.build
@@ -48,10 +48,13 @@ srcs_cairo_shared = [
 	'frame.c',
 ]
 
+dep_cairo = dependency('cairo', required: false)
+dep_libpng = dependency('libpng', required: false)
+
 deps_cairo_shared = [
 	dep_libshared,
-	dependency('cairo'),
-	dependency('libpng'),
+	dep_cairo,
+	dep_libpng,
 	dep_pixman,
 	dep_libm,
 ]
@@ -87,17 +90,21 @@ if get_option('image-webp')
 	config_h.set('HAVE_WEBP', '1')
 endif
 
-lib_cairo_shared = static_library(
-	'cairo-shared',
-	srcs_cairo_shared,
-	include_directories: common_inc,
-	dependencies: deps_cairo_shared,
-	install: false
-)
-dep_lib_cairo_shared = declare_dependency(
-	link_with: lib_cairo_shared,
-	dependencies: deps_cairo_shared
-)
+if dep_cairo.found() and dep_libpng.found()
+	lib_cairo_shared = static_library(
+		'cairo-shared',
+		srcs_cairo_shared,
+		include_directories: common_inc,
+		dependencies: deps_cairo_shared,
+		install: false
+	)
+	dep_lib_cairo_shared = declare_dependency(
+		link_with: lib_cairo_shared,
+		dependencies: deps_cairo_shared
+	)
+else
+	dep_lib_cairo_shared = dependency('', required: false)
+endif
 
 dep_matrix_c = declare_dependency(
 	sources: 'matrix.c',
